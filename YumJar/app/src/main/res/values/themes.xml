<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YumJar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Light theme background and text colors -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:textColor">@color/text_light</item>
        <!-- Additional color customizations for light mode -->
    </style>

    <style name="Theme.YumJar" parent="Base.Theme.YumJar" />
</resources>